<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- السمة الرئيسية للتطبيق، Material 3 -->
    <style name="Theme.HerbalEncyclopedia" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/brand_primary</item>
        <item name="android:navigationBarColor">@color/brand_background</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/brand_background</item>
    </style>

    <!-- سمة شاشة البداية (Splash) قبل ظهور المحتوى -->
    <style name="Theme.HerbalEncyclopedia.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/brand_primary</item>
        <item name="postSplashScreenTheme">@style/Theme.HerbalEncyclopedia</item>
    </style>

    <!-- عنوان قسم داخل بطاقة تفاصيل العشبة (الفوائد/التحذيرات/...) -->
    <style name="SectionLabel">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/brand_primary_dark</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginBottom">4dp</item>
    </style>

    <!-- نص محتوى القسم داخل بطاقة تفاصيل العشبة -->
    <style name="SectionText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#333333</item>
        <item name="android:textSize">14sp</item>
        <item name="android:lineSpacingExtra">4dp</item>
    </style>

    <!-- عنوان صف في شاشة المقارنة -->
    <style name="CompareRowTitle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/brand_primary_dark</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">14sp</item>
        <item name="android:layout_marginBottom">4dp</item>
    </style>

    <!-- حاوية صف في شاشة المقارنة (عمودان جنباً إلى جنب) -->
    <style name="CompareRowContainer">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:layout_marginBottom">14dp</item>
    </style>

    <!-- قيمة نصية داخل عمود واحد من صف المقارنة -->
    <style name="CompareRowValue">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textColor">#333333</item>
        <item name="android:textSize">13sp</item>
        <item name="android:padding">8dp</item>
        <item name="android:background">#F1F8F1</item>
        <item name="android:layout_marginEnd">4dp</item>
    </style>

</resources>
